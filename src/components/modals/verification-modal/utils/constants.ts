// Length of the verification code (e.g. 6‑digit OTP).
export const CODE_LENGTH = 6 as const;

// Index at which we render the visual dash separator in the UI.
export const VERIFICATION_DASH_INDEX = 3 as const;

